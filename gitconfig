[credential]
	helper = cache --timeout=3600
[user]
	name = YuanHong
	email = gaoyf6677@gmail.com
[alias]
    ci = commit
    co = checkout
    df = diff
    g = grep -I
    lg = log -p
    lld = !git --no-pager log --oneline --decorate --abbrev=12  -10
    lldns = !git --no-pager log --oneline --name-status --decorate -10
    st = status
    ffm = merge --ff-only
[branch "master"]
    remote = origin
    merge = refs/heads/master
    mergeoptions = --ff-only
[color]
    ui = auto
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = green
    untracked = cyan
[merge]
    log = true
[difftool "Kaleidoscope"]
    cmd = ksdiff-wrapper git \"$LOCAL\" \"$REMOTE\"
[rerere]
    enabled = 1
